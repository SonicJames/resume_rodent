# URL of the deployed Resume Rodent web app
RESUME_RODENT_APP_URL=https://your-resume-rodent-app.vercel.app

# URL of the SonicJobs search API (falls back to sample jobs if unavailable)
SONIC_JOBS_MCP_URL=https://your-sonicjobs-server.railway.app

# Port (Railway sets this automatically)
PORT=3001
